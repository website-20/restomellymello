<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="logo.jpg">
    <title>MELLY MELLO - Restaurant & Boutique Alimentaire</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Variables et réinitialisation */
        :root {
            --primary-color: #e74c3c;
            --secondary-color: #f39c12;
            --text-color: #333;
            --light-color: #f9f9f9;
            --dark-color: #2c3e50;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            color: var(--text-color);
            background-color: var(--light-color);
            line-height: 1.6;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        ul {
            list-style: none;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .btn {
            display: inline-block;
            padding: 12px 24px;
            background-color: var(--primary-color);
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: var(--transition);
            font-weight: bold;
        }

        .btn:hover {
            background-color: var(--secondary-color);
            transform: translateY(-3px);
            box-shadow: var(--shadow);
        }

        .section {
            padding: 80px 0;
        }

        .section-title {
            text-align: center;
            margin-bottom: 40px;
            font-size: 2.5rem;
            color: var(--dark-color);
            position: relative;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 80px;
            height: 4px;
            background-color: var(--primary-color);
            margin: 15px auto;
        }

        /* Header et Navigation */
        header {
            background-color: white;
            box-shadow: var(--shadow);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }

        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: var(--primary-color);
        }

        .logo span {
            color: var(--secondary-color);
        }

        .nav-menu {
            display: flex;
        }

        .nav-menu li {
            margin: 0 15px;
        }

        .nav-menu a {
            font-weight: 600;
            transition: var(--transition);
        }

        .nav-menu a:hover {
            color: var(--primary-color);
        }

        .hamburger {
            display: none;
            cursor: pointer;
            font-size: 1.5rem;
        }

        /* Bannière dynamique */
        .slider {
            position: relative;
            height: 80vh;
            overflow: hidden;
            margin-top: 70px;
        }

        .slide {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 1s ease;
            background-size: cover;
            background-position: center;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-align: center;
        }

        .slide.active {
            opacity: 1;
        }

        .slide-content {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 30px;
            border-radius: 10px;
            max-width: 700px;
        }

        .slide-content h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
        }

        .slide-content p {
            font-size: 1.2rem;
            margin-bottom: 20px;
        }

        .slider-nav {
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
        }

        .slider-indicator {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.5);
            margin: 0 8px;
            cursor: pointer;
        }

        .slider-indicator.active {
            background-color: white;
        }

        /* À propos */
        .about-content {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            gap: 40px;
        }

        .about-text {
            flex: 1;
            min-width: 300px;
        }

        .about-image {
            flex: 1;
            min-width: 300px;
            height: 400px;
            background-color: #ddd;
            border-radius: 10px;
            overflow: hidden;
        }

        .branches {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 40px;
        }

        .branch-card {
            flex: 1;
            min-width: 250px;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: var(--shadow);
            text-align: center;
            transition: var(--transition);
        }

        .branch-card:hover {
            transform: translateY(-5px);
        }

        .branch-icon {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 15px;
        }

        /* Menu gastronomie */
        .filters {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 30px;
        }

        .filter-btn {
            padding: 8px 16px;
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 4px;
            cursor: pointer;
            transition: var(--transition);
        }

        .filter-btn.active, .filter-btn:hover {
            background-color: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 30px;
        }

        .menu-item {
            background-color: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
        }

        .menu-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }

        .menu-item-image {
            height: 200px;
            background-color: #ddd;
            background-size: cover;
            background-position: center;
        }

        .menu-item-content {
            padding: 20px;
        }

        .menu-item-title {
            font-size: 1.2rem;
            margin-bottom: 10px;
        }

        .menu-item-price {
            color: var(--primary-color);
            font-weight: bold;
            font-size: 1.2rem;
            margin: 10px 0;
        }

        /* Boutique */
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 30px;
        }

        .product-card {
            background-color: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
        }

        .product-card:hover {
            transform: translateY(-5px);
        }

        .product-image {
            height: 200px;
            background-color: #ddd;
            background-size: cover;
            background-position: center;
        }

        .product-content {
            padding: 20px;
        }

        .product-title {
            font-size: 1.1rem;
            margin-bottom: 10px;
        }

        .product-price {
            color: var(--primary-color);
            font-weight: bold;
            font-size: 1.2rem;
            margin: 10px 0;
        }

        .add-to-cart {
            width: 100%;
            text-align: center;
            margin-top: 15px;
        }

        /* Panier */
        .cart-sidebar {
            position: fixed;
            top: 0;
            right: -400px;
            width: 350px;
            height: 100vh;
            background-color: white;
            box-shadow: -5px 0 15px rgba(0, 0, 0, 0.1);
            transition: var(--transition);
            z-index: 1001;
            padding: 20px;
            overflow-y: auto;
        }

        .cart-sidebar.open {
            right: 0;
        }

        .cart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #eee;
        }

        .close-cart {
            cursor: pointer;
            font-size: 1.5rem;
        }

        .cart-items {
            margin-bottom: 20px;
        }

        .cart-item {
            display: flex;
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px solid #eee;
        }

        .cart-item-image {
            width: 60px;
            height: 60px;
            background-color: #ddd;
            background-size: cover;
            background-position: center;
            border-radius: 4px;
            margin-right: 15px;
        }

        .cart-item-details {
            flex: 1;
        }

        .cart-item-title {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .cart-item-price {
            color: var(--primary-color);
        }

        .cart-item-quantity {
            display: flex;
            align-items: center;
            margin-top: 5px;
        }

        .quantity-btn {
            width: 25px;
            height: 25px;
            background-color: #f1f1f1;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .quantity-input {
            width: 40px;
            text-align: center;
            margin: 0 5px;
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 2px;
        }

        .remove-item {
            color: #ff0000;
            cursor: pointer;
            margin-left: 10px;
        }

        .cart-total {
            font-size: 1.2rem;
            font-weight: bold;
            text-align: right;
            margin: 20px 0;
        }

        .checkout-btn {
            width: 100%;
            text-align: center;
            padding: 15px;
            background-color: var(--secondary-color);
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
            transition: var(--transition);
        }

        .checkout-btn:hover {
            background-color: var(--primary-color);
        }

        .cart-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
            opacity: 0;
            visibility: hidden;
            transition: var(--transition);
        }

        .cart-overlay.open {
            opacity: 1;
            visibility: visible;
        }

        .cart-icon {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            background-color: var(--primary-color);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: var(--shadow);
            z-index: 999;
            transition: var(--transition);
        }

        .cart-icon:hover {
            background-color: var(--secondary-color);
            transform: scale(1.1);
        }

        .cart-count {
            position: absolute;
            top: -5px;
            right: -5px;
            width: 25px;
            height: 25px;
            background-color: var(--secondary-color);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
        }

        /* Contact */
        .contact-container {
            display: flex;
            flex-wrap: wrap;
            gap: 40px;
        }

        .contact-form {
            flex: 1;
            min-width: 300px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        .form-control {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 1rem;
        }

        textarea.form-control {
            min-height: 150px;
            resize: vertical;
        }

        .map {
            flex: 1;
            min-width: 300px;
            height: 400px;
            background-color: #ddd;
            border-radius: 8px;
            overflow: hidden;
        }

        /* WhatsApp flottant */
        .whatsapp-float {
            position: fixed;
            bottom: 30px;
            left: 30px;
            width: 60px;
            height: 60px;
            background-color: #25D366;
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: var(--shadow);
            z-index: 999;
            transition: var(--transition);
        }

        .whatsapp-float:hover {
            transform: scale(1.1);
        }

        /* Footer */
        footer {
            background-color: var(--dark-color);
            color: white;
            padding: 40px 0 20px;
        }

        .footer-content {
            display: flex;
            flex-wrap: wrap;
            gap: 40px;
            margin-bottom: 30px;
        }

        .footer-column {
            flex: 1;
            min-width: 200px;
        }

        .footer-column h3 {
            margin-bottom: 20px;
            position: relative;
            padding-bottom: 10px;
        }

        .footer-column h3::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 40px;
            height: 3px;
            background-color: var(--primary-color);
        }

        .footer-column ul li {
            margin-bottom: 10px;
        }

        .footer-column ul li a:hover {
            color: var(--primary-color);
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 15px;
        }

        .social-links a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            transition: var(--transition);
        }

        .social-links a:hover {
            background-color: var(--primary-color);
            transform: translateY(-3px);
        }

        .copyright {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header-container {
                flex-direction: column;
                text-align: center;
            }

            .nav-menu {
                margin-top: 20px;
                flex-direction: column;
                display: none;
            }

            .nav-menu.active {
                display: flex;
            }

            .nav-menu li {
                margin: 10px 0;
            }

            .hamburger {
                display: block;
                position: absolute;
                top: 20px;
                right: 20px;
            }

            .slide-content h2 {
                font-size: 1.8rem;
            }

            .slide-content p {
                font-size: 1rem;
            }

            .section {
                padding: 60px 0;
            }

            .section-title {
                font-size: 2rem;
            }

            .cart-sidebar {
                width: 300px;
            }
        }
    </style>
</head>
<body>
    <!-- Header et Navigation -->
    <header>
        <div class="container header-container">
            <div class="logo">MELLY <span>MELLO</span></div>
            <ul class="nav-menu">
                <li><a href="#accueil">Accueil</a></li>
                <li><a href="#about">À propos</a></li>
                <li><a href="#gastronomie">Gastronomie</a></li>
                <li><a href="#boutique">Boutique</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
            <div class="hamburger">
                <i class="fas fa-bars"></i>
            </div>
        </div>
    </header>

    <!-- Bannière dynamique -->
    <section class="slider" id="accueil">
        <div class="slide active" style="background-image: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://source.unsplash.com/random/1600x900/?restaurant');">
            <div class="slide-content">
                <h2>Bienvenue chez MELLY MELLO</h2>
                <p>Découvrez une expérience culinaire exceptionnelle alliant saveurs locales et innovation</p>
                <div>
                    <a href="#boutique" class="btn">Commander</a>
                    <a href="#gastronomie" class="btn" style="background-color: var(--secondary-color);">Découvrir nos plats</a>
                </div>
            </div>
        </div>
        <div class="slide" style="background-image: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://source.unsplash.com/random/1600x900/?food');">
            <div class="slide-content">
                <h2>Une cuisine raffinée</h2>
                <p>Des produits frais et de qualité préparés avec passion</p>
                <a href="#gastronomie" class="btn">Voir le menu</a>
            </div>
        </div>
        <div class="slide" style="background-image: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://source.unsplash.com/random/1600x900/?market');">
            <div class="slide-content">
                <h2>Produits alimentaires de qualité</h2>
                <p>Découvrez notre sélection de produits soigneusement choisis</p>
                <a href="#boutique" class="btn">Visiter la boutique</a>
            </div>
        </div>
        <div class="slider-nav">
            <div class="slider-indicator active"></div>
            <div class="slider-indicator"></div>
            <div class="slider-indicator"></div>
        </div>
    </section>

    <!-- À propos -->
    <section class="section" id="about">
        <div class="container">
            <h2 class="section-title">À propos de nous</h2>
            <div class="about-content">
                <div class="about-text">
                    <h3>Notre histoire</h3>
                    <p>MELLY MELLO a été fondée par GNAHOUAME Alali Delali Odile avec une passion pour la gastronomie et le désir de partager les saveurs authentiques de notre terroir avec le plus grand nombre.</p>
                    <p>Depuis nos débuts, nous nous engageons à offrir à nos clients des produits de la plus haute qualité, préparés avec soin et créativité.</p>

                    <h3>Nos valeurs</h3>
                    <p>Qualité, authenticité et innovation sont au cœur de notre démarche. Nous sélectionnons rigoureusement nos ingrédients et mettons un point d'honneur à proposer une expérience culinaire unique à chacun de nos clients.</p>
                </div>
                <div class="about-image">
                    <img src="logo.jpg" alt="Équipe MELLY MELLO" style="width: 100%; height: 100%; object-fit: cover;">
                </div>
            </div>

            <div class="branches">
                <div class="branch-card">
                    <div class="branch-icon">
                        <i class="fas fa-utensils"></i>
                    </div>
                    <h3>Restauration</h3>
                    <p>Notre restaurant vous propose une cuisine raffinée alliant traditions et modernité. Des plats savoureux préparés avec des ingrédients frais et locaux.</p>
                </div>
                <div class="branch-card">
                    <div class="branch-icon">
                        <i class="fas fa-store"></i>
                    </div>
                    <h3>Boutique alimentaire</h3>
                    <p>Découvrez dans notre boutique une sélection de produits alimentaires de qualité : épices, condiments, préparations maison et bien plus encore.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Gastronomie -->
    <section class="section" id="gastronomie" style="background-color: #f5f5f5;">
        <div class="container">
            <h2 class="section-title">Notre Gastronomie</h2>

            <div class="filters">
                <button class="filter-btn active" data-filter="all">Tous</button>
                <button class="filter-btn" data-filter="entrees">Entrées</button>
                <button class="filter-btn" data-filter="plats">Plats principaux</button>
                <button class="filter-btn" data-filter="desserts">Desserts</button>
                <button class="filter-btn" data-filter="boissons">Boissons</button>
            </div>

            <div class="menu-grid">
                <!-- Les éléments du menu seront générés dynamiquement par JavaScript -->
            </div>
        </div>
    </section>

    <!-- Boutique -->
    <section class="section" id="boutique">
        <div class="container">
            <h2 class="section-title">Notre Boutique</h2>

            <div class="products-grid">
                <!-- Les produits seront générés dynamiquement par JavaScript -->
            </div>
        </div>
    </section>

    <!-- Contact -->
    <section class="section" id="contact" style="background-color: #f5f5f5;">
        <div class="container">
            <h2 class="section-title">Contact & Localisation</h2>

            <div class="contact-container">
                <div class="contact-form">
                    <form id="contactForm">
                        <div class="form-group">
                            <label for="name">Nom complet</label>
                            <input type="text" id="name" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="email">Email</label>
                            <input type="email" id="email" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="phone">Téléphone</label>
                            <input type="tel" id="phone" class="form-control">
                        </div>
                        <div class="form-group">
                            <label for="message">Message</label>
                            <textarea id="message" class="form-control" required></textarea>
                        </div>
                        <button type="submit" class="btn">Envoyer le message</button>
                    </form>
                </div>

                <div class="map">
                    <!-- Intégration de Google Maps -->
                    <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3960.27837495776!2d1.209623215759013!3d6.902995295080863!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zNsKwNTQnMTAuOCJOIDHCsDEyJzM0LjgiRQ!5e1!3m2!1sfr!2stg!4v1624456789012!5m2!1sfr!2stg" width="100%" height="100%" style="border:0;" allowfullscreen="" loading="lazy"></iframe>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-column">
                    <h3>MELLY MELLO</h3>
                    <p>Restaurant et boutique alimentaire proposant des produits de qualité et une expérience culinaire unique.</p>
                </div>

                <div class="footer-column">
                    <h3>Liens rapides</h3>
                    <ul>
                        <li><a href="#accueil">Accueil</a></li>
                        <li><a href="#about">À propos</a></li>
                        <li><a href="#gastronomie">Gastronomie</a></li>
                        <li><a href="#boutique">Boutique</a></li>
                        <li><a href="#contact">Contact</a></li>
                    </ul>
                </div>

                <div class="footer-column">
                    <h3>Contact</h3>
                    <ul>
                        <li><i class="fas fa-map-marker-alt"></i> Adresse: Lomé, Togo</li>
                        <li><i class="fas fa-phone"></i> Téléphone: +228 XX XX XX XX</li>
                        <li><i class="fas fa-envelope"></i> Email: contact@mellymello.tg</li>
                    </ul>
                </div>

                <div class="footer-column">
                    <h3>Suivez-nous</h3>
                    <div class="social-links">
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-whatsapp"></i></a>
                    </div>
                </div>
            </div>

            <div class="copyright">
                <p>&copy; 2023 MELLY MELLO. Tous droits réservés.</p>
            </div>
        </div>
    </footer>

    <!-- Panier -->
    <div class="cart-overlay"></div>
    <div class="cart-sidebar">
        <div class="cart-header">
            <h3>Votre Panier</h3>
            <div class="close-cart">
                <i class="fas fa-times"></i>
            </div>
        </div>
        <div class="cart-items">
            <!-- Les éléments du panier seront générés dynamiquement par JavaScript -->
        </div>
        <div class="cart-total">
            Total: <span id="cart-total-amount">0</span> FCFA
        </div>
        <button class="checkout-btn" id="checkout-btn">Commander via WhatsApp</button>
    </div>

    <!-- Icône du panier -->
    <div class="cart-icon">
        <i class="fas fa-shopping-cart"></i>
        <div class="cart-count">0</div>
    </div>

    <!-- Bouton WhatsApp flottant -->
    <a href="https://wa.me/228XXXXXXXX?text=Bonjour%20MELLY%20MELLO,%20j'aimerais%20avoir%20plus%20d'informations..." class="whatsapp-float" target="_blank">
        <i class="fab fa-whatsapp" style="font-size: 1.8rem;"></i>
    </a>

    <script>
        // Données des plats (simulées)
        const menuItems = [
            {
                id: 1,
                name: "Ayimolou",
                description: "Riz avec du haricot plus acompagnement de votre choix",
                price: 4500,
                category: "plats",
                image: "https://saveurstogo.com/wp-content/uploads/2019/09/cm1w8pxxe00060ck0h79jcg4p.jpg"
            },
            {
                id: 2,
                name: "Akoume",
                description: "Pate avec farine de mais plus sauce de votre choix",
                price: 6500,
                category: "plats",
                image: "https://quefairealome.com/wp-content/uploads/2023/10/maxresdefault-2-2.jpg"
            },
            {
                id: 3,
                name: "Kohm",
                description: "Poisson frais braisé au feu de bois, servi avec alloco et attiéké",
                price: 7500,
                category: "plats",
                image: "https://www.elviadelices.fr/cdn/shop/files/Plat_cuisin_dokounou.jpg?v=1747949120&width=1445"
            },
            {
                id: 4,
                name: "Frites au poulet ",
                description: "Délicieux poulet braisé avec des frites",
                price: 3500,
                category: "desserts",
                image: "https://www.hachette-vins.com//uploads/media/recettes/0001/09/6b6ba9e618a230e9bc30971078ec4bae082e973a.jpeg"
            },
            {
                id: 5,
                name: "Jus de Bissap Naturel",
                description: "Jus naturel d'hibiscus sans conservateur",
                price: 2000,
                category: "boissons",
                image: "https://recettesdafrique.com/wp-content/uploads/2021/08/Jus-de-bissap-ou-dhibiscus-a-la-cannelle.jpg"
            },
            {
                id: 6,
                name: "Sauce Gombo",
                description: "Sauce traditionnelle à base de gombo frais",
                price: 3000,
                category: "plats",
                image: "https://i-re.unimedias.fr/2025/06/05/r125-recette-africaine-togo-sauce-gombo-boeuf-crevettes_co.jpg?auto=format%2Ccompress&crop=faces&cs=tinysrgb&fit=crop&h=501&w=890"
            }
        ];

        // Données des produits (simulées)
        const products = [
            {
                id: 101,
                name: "Épices Locales",
                description: "Mélange d'épices locales pour rehausser vos plats",
                price: 2500,
                image: "https://source.unsplash.com/random/300x200/?spices"
            },
            {
                id: 102,
                name: "Miel Naturel",
                description: "Miel pur de la région, non pasteurisé",
                price: 5000,
                image: "https://source.unsplash.com/random/300x200/?honey"
            },
            {
                id: 103,
                name: "Sac de riz",
                description: "Sacs de 50 kg , 25 kg et 5 kg",
                price: 4000,
                image: "https://www.emadex.biz/boutique/wp-content/uploads/2018/04/sac-de-riz-600x600.jpg"
            },
            {
                id: 104,
                name: "Ebéssé fionfion(piments noir)",
                description: "Piments noir très doux",
                price: 3500,
                image: "https://source.unsplash.com/random/300x200/?chocolate"
            }
        ];

        // Gestion du slider
        let currentSlide = 0;
        const slides = document.querySelectorAll('.slide');
        const indicators = document.querySelectorAll('.slider-indicator');

        function showSlide(index) {
            slides.forEach(slide => slide.classList.remove('active'));
            indicators.forEach(indicator => indicator.classList.remove('active'));

            slides[index].classList.add('active');
            indicators[index].classList.add('active');
            currentSlide = index;
        }

        function nextSlide() {
            let next = currentSlide + 1;
            if (next >= slides.length) next = 0;
            showSlide(next);
        }

        // Configuration du slider
        indicators.forEach((indicator, index) => {
            indicator.addEventListener('click', () => showSlide(index));
        });

        setInterval(nextSlide, 5000);

        // Gestion du menu de navigation mobile
        const hamburger = document.querySelector('.hamburger');
        const navMenu = document.querySelector('.nav-menu');

        hamburger.addEventListener('click', () => {
            navMenu.classList.toggle('active');
        });

        // Génération des éléments du menu
        const menuGrid = document.querySelector('.menu-grid');

        function renderMenuItems(filter = 'all') {
            menuGrid.innerHTML = '';

            const filteredItems = filter === 'all'
                ? menuItems
                : menuItems.filter(item => item.category === filter);

            filteredItems.forEach(item => {
                const menuItem = document.createElement('div');
                menuItem.className = 'menu-item';
                menuItem.innerHTML = `
                    <div class="menu-item-image" style="background-image: url('${item.image}')"></div>
                    <div class="menu-item-content">
                        <h3 class="menu-item-title">${item.name}</h3>
                        <p>${item.description}</p>
                        <div class="menu-item-price">${item.price.toLocaleString()} FCFA</div>
                        <button class="btn add-to-cart-btn" data-id="${item.id}" data-name="${item.name}" data-price="${item.price}" data-image="${item.image}">Ajouter au panier</button>
                    </div>
                `;
                menuGrid.appendChild(menuItem);
            });

            // Ajouter les écouteurs d'événements pour les boutons d'ajout au panier
            document.querySelectorAll('.add-to-cart-btn').forEach(button => {
                button.addEventListener('click', addToCart);
            });
        }

        // Génération des produits de la boutique
        const productsGrid = document.querySelector('.products-grid');

        function renderProducts() {
            productsGrid.innerHTML = '';

            products.forEach(product => {
                const productCard = document.createElement('div');
                productCard.className = 'product-card';
                productCard.innerHTML = `
                    <div class="product-image" style="background-image: url('${product.image}')"></div>
                    <div class="product-content">
                        <h3 class="product-title">${product.name}</h3>
                        <p>${product.description}</p>
                        <div class="product-price">${product.price.toLocaleString()} FCFA</div>
                        <button class="btn add-to-cart add-to-cart-btn" data-id="${product.id}" data-name="${product.name}" data-price="${product.price}" data-image="${product.image}">Ajouter au panier</button>
                    </div>
                `;
                productsGrid.appendChild(productCard);
            });

            // Ajouter les écouteurs d'événements pour les boutons d'ajout au panier
            document.querySelectorAll('.add-to-cart-btn').forEach(button => {
                button.addEventListener('click', addToCart);
            });
        }

        // Filtrage des plats
        const filterButtons = document.querySelectorAll('.filter-btn');

        filterButtons.forEach(button => {
            button.addEventListener('click', () => {
                filterButtons.forEach(btn => btn.classList.remove('active'));
                button.classList.add('active');
                renderMenuItems(button.dataset.filter);
            });
        });

        // Gestion du panier
        let cart = JSON.parse(localStorage.getItem('cart')) || [];
        const cartIcon = document.querySelector('.cart-icon');
        const cartCount = document.querySelector('.cart-count');
        const cartSidebar = document.querySelector('.cart-sidebar');
        const cartOverlay = document.querySelector('.cart-overlay');
        const closeCart = document.querySelector('.close-cart');
        const cartItemsContainer = document.querySelector('.cart-items');
        const cartTotalAmount = document.getElementById('cart-total-amount');
        const checkoutBtn = document.getElementById('checkout-btn');

        function updateCart() {
            // Mettre à jour le compteur du panier
            const totalItems = cart.reduce((total, item) => total + item.quantity, 0);
            cartCount.textContent = totalItems;

            // Sauvegarder le panier dans le localStorage
            localStorage.setItem('cart', JSON.stringify(cart));

            // Mettre à jour l'affichage du panier
            renderCartItems();
        }

        function renderCartItems() {
            cartItemsContainer.innerHTML = '';

            if (cart.length === 0) {
                cartItemsContainer.innerHTML = '<p class="empty-cart">Votre panier est vide</p>';
                cartTotalAmount.textContent = '0';
                return;
            }

            let total = 0;

            cart.forEach(item => {
                const itemTotal = item.price * item.quantity;
                total += itemTotal;

                const cartItem = document.createElement('div');
                cartItem.className = 'cart-item';
                cartItem.innerHTML = `
                    <div class="cart-item-image" style="background-image: url('${item.image}')"></div>
                    <div class="cart-item-details">
                        <h4 class="cart-item-title">${item.name}</h4>
                        <div class="cart-item-price">${item.price.toLocaleString()} FCFA x ${item.quantity}</div>
                        <div class="cart-item-quantity">
                            <button class="quantity-btn decrease-btn" data-id="${item.id}">-</button>
                            <input type="number" class="quantity-input" value="${item.quantity}" min="1" data-id="${item.id}">
                            <button class="quantity-btn increase-btn" data-id="${item.id}">+</button>
                            <span class="remove-item" data-id="${item.id}"><i class="fas fa-trash"></i></span>
                        </div>
                    </div>
                `;
                cartItemsContainer.appendChild(cartItem);
            });

            cartTotalAmount.textContent = total.toLocaleString();

            // Ajouter les écouteurs d'événements pour les boutons de quantité
            document.querySelectorAll('.increase-btn').forEach(button => {
                button.addEventListener('click', increaseQuantity);
            });

            document.querySelectorAll('.decrease-btn').forEach(button => {
                button.addEventListener('click', decreaseQuantity);
            });

            document.querySelectorAll('.quantity-input').forEach(input => {
                input.addEventListener('change', changeQuantity);
            });

            document.querySelectorAll('.remove-item').forEach(button => {
                button.addEventListener('click', removeItem);
            });
        }

        function addToCart(e) {
            const id = parseInt(e.target.dataset.id);
            const name = e.target.dataset.name;
            const price = parseInt(e.target.dataset.price);
            const image = e.target.dataset.image;

            const existingItem = cart.find(item => item.id === id);

            if (existingItem) {
                existingItem.quantity += 1;
            } else {
                cart.push({
                    id,
                    name,
                    price,
                    image,
                    quantity: 1
                });
            }

            updateCart();

            // Animation d'ajout au panier
            e.target.textContent = 'Ajouté !';
            setTimeout(() => {
                e.target.textContent = 'Ajouter au panier';
            }, 1500);
        }

        function increaseQuantity(e) {
            const id = parseInt(e.target.dataset.id);
            const item = cart.find(item => item.id === id);

            if (item) {
                item.quantity += 1;
                updateCart();
            }
        }

        function decreaseQuantity(e) {
            const id = parseInt(e.target.dataset.id);
            const item = cart.find(item => item.id === id);

            if (item && item.quantity > 1) {
                item.quantity -= 1;
                updateCart();
            }
        }

        function changeQuantity(e) {
            const id = parseInt(e.target.dataset.id);
            const item = cart.find(item => item.id === id);
            const newQuantity = parseInt(e.target.value);

            if (item && newQuantity > 0) {
                item.quantity = newQuantity;
                updateCart();
            }
        }

        function removeItem(e) {
            const id = parseInt(e.target.closest('.remove-item').dataset.id);
            cart = cart.filter(item => item.id !== id);
            updateCart();
        }

        // Ouvrir/fermer le panier
        cartIcon.addEventListener('click', () => {
            cartSidebar.classList.add('open');
            cartOverlay.classList.add('open');
        });

        closeCart.addEventListener('click', () => {
            cartSidebar.classList.remove('open');
            cartOverlay.classList.remove('open');
        });

        cartOverlay.addEventListener('click', () => {
            cartSidebar.classList.remove('open');
            cartOverlay.classList.remove('open');
        });

        // Commander via WhatsApp
        checkoutBtn.addEventListener('click', () => {
            if (cart.length === 0) return;

            let message = "Bonjour MELLY MELLO, je veux commander:%0A%0A";

            cart.forEach(item => {
                message += `- ${item.quantity}x ${item.name} : ${(item.price * item.quantity).toLocaleString()} FCFA%0A`;
            });

            const total = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
            message += `%0ATotal: ${total.toLocaleString()} FCFA`;

            const phoneNumber = "228XXXXXXXX"; // Remplacez par le vrai numéro
            const whatsappURL = `https://wa.me/${phoneNumber}?text=${message}`;

            window.open(whatsappURL, '_blank');
        });

        // Formulaire de contact
        const contactForm = document.getElementById('contactForm');

        contactForm.addEventListener('submit', (e) => {
            e.preventDefault();

            // Ici, vous ajouteriez le code pour envoyer le formulaire
            alert('Merci pour votre message ! Nous vous contacterons bientôt.');
            contactForm.reset();
        });

        // Initialisation
        document.addEventListener('DOMContentLoaded', () => {
            renderMenuItems();
            renderProducts();
            updateCart();
        });
    </script>
</body>
</html>
